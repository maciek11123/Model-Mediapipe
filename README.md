# Model-Mediapipe Tracker

This zip contains the updated `index.html` application we just generated.

## Next Steps

To make this web app fully functional, you **must** place your custom engine and model files into this exact directory before running the local server. 

**Required Files to add:**
- `rig_engine.js`
- `apply_calibration.js`
- `Bluey_fixed_1.glb`
- `Bingo_fixed_1.glb`
- `kuromi_fixed_1.glb`
- `Pompompurin_fixed_1.glb`

*(Note: I attempted to fetch these from your GitHub repository `https://github.com/maciek11123/Model-Mediapipe.git`, but the repository is currently empty!)*

Once those files are here, you can run a local server (e.g., `python -m http.server 8000`) and the application will work as intended.
